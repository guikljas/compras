const crypto=require('node:crypto');
const roles={Administrador:3,Comprador:2,Consulta:1};
const entities=new Set(['suppliers','products','invoices','users','plans','companies','categories']);
const clean=x=>String(x??'').trim();
const env=name=>{const value=process.env[name];if(!value)throw new Error(`Variável ${name} não configurada.`);return value};
const headers={'X-Content-Type-Options':'nosniff','X-Frame-Options':'DENY','Referrer-Policy':'strict-origin-when-cross-origin','Permissions-Policy':'camera=(), microphone=(), geolocation=()','Content-Security-Policy':"default-src 'self'; img-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; script-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'"};
const send=(res,status,payload,extra={})=>{res.status(status).set({...headers,'Cache-Control':'no-store',...extra}).json(payload)};
const parseBody=req=>typeof req.body==='object'&&req.body!==null?req.body:typeof req.body==='string'?JSON.parse(req.body||'{}'):{};
const cookie=(req,name)=>(req.headers.cookie||'').split(';').map(x=>x.trim()).find(x=>x.startsWith(`${name}=`))?.slice(name.length+1);
const hashPassword=password=>{const salt=crypto.randomBytes(16).toString('hex');return `${salt}:${crypto.scryptSync(password,salt,64).toString('hex')}`};
const verifyPassword=(password,saved)=>{const [salt,hash]=String(saved).split(':');if(!salt||!hash)return false;return crypto.timingSafeEqual(Buffer.from(hash,'hex'),Buffer.from(crypto.scryptSync(password,salt,64).toString('hex')))};
async function db(table,{method='GET',query={},body,prefer}={}){const url=new URL(`/rest/v1/${table}`,env('SUPABASE_URL'));for(const [key,value] of Object.entries(query))if(value!==undefined&&value!==null)url.searchParams.set(key,value);const key=env('SUPABASE_SECRET_KEY');const response=await fetch(url,{method,headers:{apikey:key,Authorization:`Bearer ${key}`,...(body?{'Content-Type':'application/json'}:{}),...(prefer?{Prefer:prefer}:{})},body:body?JSON.stringify(body):undefined});const text=await response.text();let result;try{result=text?JSON.parse(text):null}catch{result=text}if(!response.ok)throw new Error(typeof result==='object'?(result.message||result.hint||'Erro no banco de dados.'):'Erro no banco de dados.');return {data:result,headers:response.headers}}
async function one(table,query){const {data}=await db(table,{query:{...query,limit:1}});return data?.[0]||null}
async function ensureAdmin(){const email=clean(process.env.ADMIN_EMAIL||'admin@erimax.com').toLowerCase();if(await one('users',{email:`eq.${email}`}))return;const password=String(process.env.ADMIN_PASSWORD||'');if(password.length<12)throw new Error('Defina ADMIN_PASSWORD com ao menos 12 caracteres na Vercel.');await db('users',{method:'POST',body:{name:'Administrador',email,password_hash:hashPassword(password),role:'Administrador'},prefer:'return=minimal'})}
async function currentUser(req){const token=cookie(req,'cc_session');if(!token)return null;const session=await one('sessions',{token_hash:`eq.${crypto.createHash('sha256').update(token).digest('hex')}`,expires_at:`gt.${Date.now()}`});if(!session)return null;return one('users',{id:`eq.${session.user_id}`,active:'eq.true'})}
const requireUser=async(req,res,min=1)=>{const user=await currentUser(req);if(!user){send(res,401,{error:'Sessão inválida ou expirada.'});return null}if(roles[user.role]<min){send(res,403,{error:'Você não tem permissão para esta ação.'});return null}return user};
const fields={suppliers:['name','cnpj','contact','phone','email'],products:['name','code','category','unit'],plans:['code','name'],companies:['code','name','cnpj'],categories:['name','description'],invoices:['number','date','supplier','sector','plan_code','total','purchase_location','company','expense_type','product_name','quantity']};
const loginAttempts=new Map();
function allowed(req){const ip=(req.headers['x-forwarded-for']||req.socket?.remoteAddress||'unknown').split(',')[0].trim(),now=Date.now(),previous=loginAttempts.get(ip);if(!previous||now-previous.time>900000){loginAttempts.set(ip,{time:now,count:1});return true}if(previous.count>=10)return false;previous.count++;return true}
function csvRows(text){const rows=[];let row=[],value='',quoted=false;for(let i=0;i<text.length;i++){const c=text[i];if(c==='"'){if(quoted&&text[i+1]==='"'){value+='"';i++}else quoted=!quoted}else if(!quoted&&';|,'.includes(c)){row.push(value.trim());value=''}else if(!quoted&&(c==='\n'||c==='\r')){if(c==='\r'&&text[i+1]==='\n')i++;row.push(value.trim());if(row.some(Boolean))rows.push(row);row=[];value=''}else value+=c}row.push(value.trim());if(row.some(Boolean))rows.push(row);return rows}
const norm=value=>String(value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]/g,'');
async function importCsv(text){if(typeof text!=='string'||text.length>3_000_000)throw new Error('Envie um CSV de até 3 MB.');const rows=csvRows(text.replace(/^\uFEFF/,''));if(rows.length<2)throw new Error('O CSV precisa ter cabeçalho e ao menos uma linha.');const header=rows.shift().map(norm),index=(...names)=>header.findIndex(h=>names.includes(h)),ix={date:index('data','datacompra'),location:index('localdecompra','local'),company:index('qualempresa','empresa'),sector:index('setor'),expense:index('tipodedespesa','despesa'),product:index('produto'),quantity:index('quantidade','qtd'),total:index('valortotal','valor')};if(Object.values(ix).some(x=>x<0))throw new Error('Use o modelo de colunas disponibilizado.');const entries=rows.map((row,n)=>{const get=k=>clean(row[ix[k]]),raw=get('date'),date=/^\d{2}\/\d{2}\/\d{4}$/.test(raw)?`${raw.slice(6)}-${raw.slice(3,5)}-${raw.slice(0,2)}`:raw,total=Number(get('total').replace(/\./g,'').replace(',','.')),quantity=Number(get('quantity').replace(',','.'));if(!date||!get('location')||!get('company')||!get('sector')||!get('expense')||!get('product')||!Number.isFinite(total)||!Number.isFinite(quantity))throw new Error(`Linha ${n+2}: confira os campos.`);return {date,location:get('location'),company:get('company').toUpperCase(),sector:get('sector'),expense:get('expense'),product:get('product'),total,quantity}});for(const x of entries){const upsert=async(table,match,body)=>{if(!await one(table,match))await db(table,{method:'POST',body,prefer:'return=minimal'})};await upsert('companies',{code:`eq.${x.company}`},{code:x.company,name:x.company});await upsert('categories',{name:`eq.${x.expense}`},{name:x.expense});await upsert('suppliers',{name:`eq.${x.location}`},{name:x.location});await upsert('products',{name:`eq.${x.product}`},{name:x.product,category:x.expense,unit:'UN'});let plan=await one('plans',{or:`(code.eq.${x.sector.toUpperCase()},name.eq.${x.sector})`});if(!plan){let code=x.sector.toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,20)||'SETOR';plan=await one('plans',{code:`eq.${code}`});if(!plan){await db('plans',{method:'POST',body:{code,name:x.sector},prefer:'return=representation'});plan=await one('plans',{code:`eq.${code}`})}}await db('invoices',{method:'POST',body:{number:`CSV-${Date.now()}-${crypto.randomUUID().slice(0,6)}`,date:x.date,supplier:x.location,sector:plan.code,plan_code:plan.code,total:x.total,purchase_location:x.location,company:x.company,expense_type:x.expense,product_name:x.product,quantity:x.quantity},prefer:'return=minimal'})}return entries.length}
module.exports=async(req,res)=>{try{await ensureAdmin();const url=new URL(req.url,'https://local'),path=url.pathname.replace(/^\/api\/?/,'').split('/').filter(Boolean),[entity,id]=path,method=req.method;if(path[0]==='health')return send(res,200,{ok:true});if(entity==='auth'&&id==='login'&&method==='POST'){if(!allowed(req))return send(res,429,{error:'Muitas tentativas. Aguarde alguns minutos.'},{'Retry-After':'900'});const {email,password}=parseBody(req),user=await one('users',{email:`eq.${clean(email).toLowerCase()}`,active:'eq.true'});if(!user||!verifyPassword(password||'',user.password_hash))return send(res,401,{error:'E-mail ou senha incorretos.'});const token=crypto.randomBytes(32).toString('base64url');await db('sessions',{method:'POST',body:{token_hash:crypto.createHash('sha256').update(token).digest('hex'),user_id:user.id,expires_at:Date.now()+8*3600*1000},prefer:'return=minimal'});return send(res,200,{user:{id:user.id,name:user.name,email:user.email,role:user.role}},{'Set-Cookie':`cc_session=${token}; HttpOnly; Secure; SameSite=Strict; Path=/; Max-Age=28800`})}if(entity==='auth'&&id==='logout'&&method==='POST'){const token=cookie(req,'cc_session');if(token)await db('sessions',{method:'DELETE',query:{token_hash:`eq.${crypto.createHash('sha256').update(token).digest('hex')}`}});return send(res,200,{ok:true},{'Set-Cookie':'cc_session=; HttpOnly; Secure; SameSite=Strict; Path=/; Max-Age=0'})}if(entity==='auth'&&id==='me'&&method==='GET'){const user=await requireUser(req,res);if(user)send(res,200,{user});return}const user=await requireUser(req,res);if(!user)return;if(entity==='invoices'&&id==='import-csv'&&method==='POST'){if(roles[user.role]<2)return send(res,403,{error:'Sem permissão.'});return send(res,201,{imported:await importCsv(parseBody(req).csv)})}if(!entities.has(entity))return send(res,404,{error:'Recurso não encontrado.'});if(method==='GET'){if(entity==='users'&&roles[user.role]<3)return send(res,403,{error:'Apenas administradores podem consultar usuários.'});if(entity==='products'){const page=Math.max(1,Number(url.searchParams.get('page'))||1),limit=Math.min(100,Math.max(10,Number(url.searchParams.get('limit'))||50)),q=clean(url.searchParams.get('q')).replace(/[(),%*]/g,'');const query={select:'*',order:'name.asc,id.asc',limit,offset:(page-1)*limit};if(q)query.or='(name.ilike.*'+q+'*,code.ilike.*'+q+'*,category.ilike.*'+q+'*)';const result=await db('products',{query,prefer:'count=exact'}),total=Number((result.headers.get('content-range')||'*/0').split('/')[1]);return send(res,200,{items:result.data||[],total,page,limit,pages:Math.max(1,Math.ceil(total/limit))})}const query={select:entity==='users'?'id,name,email,role,active,created_at':'*',order:entity==='plans'?'code.asc':'id.desc'};if(entity==='plans')query.active='eq.true';const result=await db(entity,{query});return send(res,200,{items:result.data||[]})}if(method==='POST'){const min=entity==='users'?3:2;if(roles[user.role]<min)return send(res,403,{error:'Você não tem permissão para este cadastro.'});const value=parseBody(req);if(entity==='users'){const email=clean(value.email).toLowerCase(),password=String(value.password||'');if(!clean(value.name)||!/^\S+@\S+\.\S+$/.test(email)||!roles[value.role]||password.length<8)return send(res,400,{error:'Informe os dados obrigatórios.'});const result=await db('users',{method:'POST',body:{name:clean(value.name),email,password_hash:hashPassword(password),role:value.role},prefer:'return=representation'});return send(res,201,{id:result.data[0].id})}if(entity==='invoices'){const planCode=clean(value.planCode),total=Number(value.total),quantity=Number(value.quantity);if(!clean(value.number)||!clean(value.date)||!clean(value.supplier)||!planCode||!Number.isFinite(total)||total<0)return send(res,400,{error:'Preencha os dados obrigatórios da compra.'});const result=await db('invoices',{method:'POST',body:{number:clean(value.number),date:clean(value.date),supplier:clean(value.supplier),sector:planCode,plan_code:planCode,total,purchase_location:clean(value.supplier),company:clean(value.company).toUpperCase(),expense_type:clean(value.expenseType),product_name:clean(value.productName),quantity:Number.isFinite(quantity)?quantity:0},prefer:'return=representation'});return send(res,201,{id:result.data[0].id})}const input={};for(const key of fields[entity]||[])input[key]=clean(value[key]);if(!input.name)return send(res,400,{error:'O nome é obrigatório.'});if(entity==='companies')input.code=input.code.toUpperCase();const result=await db(entity,{method:'POST',body:input,prefer:'return=representation'});return send(res,201,{id:result.data[0].id})}if(method==='PATCH'&&id){if(roles[user.role]<2)return send(res,403,{error:'Você não tem permissão para editar este cadastro.'});if(!fields[entity])return send(res,405,{error:'Este registro não pode ser editado.'});const value=parseBody(req),input={};for(const key of fields[entity])input[key]=key==='total'||key==='quantity'?Number(value[key]||0):clean(value[key]);if(entity==='invoices'){const code=clean(value.planCode||value.plan_code);input.sector=code;input.plan_code=code;input.purchase_location=input.supplier}if(entity==='plans'||entity==='companies')input.code=input.code.toUpperCase();await db(entity,{method:'PATCH',query:{id:`eq.${id}`},body:input,prefer:'return=minimal'});return send(res,200,{ok:true})}return send(res,405,{error:'Método não permitido.'})}catch(error){console.error(error);return send(res,500,{error:error.message||'Erro interno.'})}};
